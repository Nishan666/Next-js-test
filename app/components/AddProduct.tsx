'use client';
import React from 'react'

const AddProduct = () => {
  return (
    <div>
        <button className='btn btn-success' onClick={()=>console.log("Hello")
        }>Add Product</button>
    </div>
  )
}

export default AddProduct